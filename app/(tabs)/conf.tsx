import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Conf = () => {
    return (
        <View>
            <Text>Конфіденційність</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Conf;
